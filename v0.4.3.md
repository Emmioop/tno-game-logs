# v0.4.3 更新日志 · 修复"时代风貌"Tab 不可用

> 发布日期：2026-08-03
> 版本号：SAVE_VERSION 17 · game.js?v=47 · ui.js?v=83 · style.css?v=78

## 🐛 Bug 修复（3 个致命问题）

### 1. `renderTab` 无限递归 → Tab 点击无反应
**根因**：补丁在首次调用时才缓存 `UI._origRenderTab = UI.renderTab`，但此时 `UI.renderTab` **已经是补丁函数本身**，导致下次调用时无限递归、栈溢出，Tab 点击完全失效。

**修复**：在 IIFE 顶部、重写之前就保存所有原始函数引用：
```js
var _origRenderTab     = UI.renderTab;     // ✅ 先保存，再覆盖
var _origShowEventModal = UI.showEventModal;
var _origRenderEventLog = UI.renderEventLog;
var _origShowNextEvent  = UI.showNextEvent;
var _origNextTurn       = UI.nextTurn;
var _origProcessTurn     = UI.processTurnEvents;
```

### 2. `tab-flavor` 容器永远 `display:none`
**根因**：`index.html` 写死 `style="display:none"`，而 `renderTab` 补丁只操作 `#tab-content`，从没把 flavor 容器切回 `display:block`。

**修复**：`renderTab` 入口统一控制显隐：
```js
// 先全部隐藏
content.style.display   = 'none';
flavorTab.style.display = 'none';
// 按路由打开对应容器
if (tabName === 'flavor') {
  flavorTab.style.display = 'block';
  UI.renderFlavorLog();
}
```

### 3. `classifyDirection` 漏读 `body` 字段
**根因**：大量事件把描述放在 `ev.body` 里，但分类函数只读 `ev.desc` 和 `ev.text`，导致这些事件全部落入 `other`，徽章显示错误。

**修复**：覆盖所有描述字段：
```js
const text = (ev.title||'') + ' ' + (ev.desc||'') + ' ' +
            (ev.text||'')  + ' ' + (ev.body||'') + ' ' + (ev.content||'');
```

## ✅ 修复后测试结果

```
=== 原始函数保存 ===
  ✅ _origRenderTab / _origShowEventModal / _origRenderEventLog
  ✅ _origShowNextEvent / _origNextTurn / _origProcessTurn

=== 模拟点击"时代风貌"Tab ===
  ✅ tab-flavor display = "block"
  ✅ innerHTML 长度 704（有内容）
  ✅ 包含 flavor-card
  ✅ 包含徽章 HTML
  ✅ 包含"登月""啤酒节""天皇"等条目

=== 模拟点击"事件"Tab ===
  ✅ tab-content display = "block"
  ✅ tab-flavor display = "none"（正确隐藏）

=== 方向分类（18 个用例）===
  18/18 全部通过，含 body 字段事件正确识别

=== 语法检查 ===
  ✅ game.js / ui.js / save_system.js 全部通过
```

## 📊 对比

| 问题 | v0.4.2 | v0.4.3 |
|------|---------|---------|
| 点击"时代风貌"Tab | ❌ 无反应/空白 | ✅ 正常显示风味卡片 |
| 事件方向徽章 | ❌ 大量事件归"其他" | ✅ 18/18 正确分类 |
| `renderTab` 递归 | ❌ 栈溢出 | ✅ 正确委托原始函数 |
| Tab 容器显隐 | ❌ 写死 none | ✅ JS 统一控制 |

## 📝 文件变更

- `js/game.js` — `classifyDirection` 补全 `body`/`content` 字段
- `js/ui.js` — 重写补丁 IIFE：先保存原始函数，再覆盖；`renderTab` 正确控制双容器显隐
- `index.html` — `tab-flavor` 默认 `display:none`（由 JS 控制）

## ⚠️ 升级注意
- SAVE_VERSION 仍为 17（向下兼容 v0.4.2 存档）
- 只需覆盖上传 5 个文件即可
