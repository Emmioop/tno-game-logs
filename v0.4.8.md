# v0.4.8 更新日志 · Debug 模式最终定型 + 杉斯模拟器竞品分析 + 第二 Boss 预备

> 发布日期：2026-08-09
> 代码提交：`be3f082`
> 部署地址：https://emmioop.github.io/tno-strategy-game/sans/index.html

## 🎯 核心改动

Sans BOSS 战 Debug 模式从"定时器轮询"升级为"运行时 hook EventVariable.setValue"，彻底解决了之前"扣血+defeat 在同一帧完成、定时器赶不上"的问题。同时完成对 5 款主流 Sans 模拟器的竞品分析，明确了资源体积差距的根因。

## 🐛 Debug 模式迭代史（踩坑实录）

### 第一版：TNO UndertaleBattle 子模式
- 路径：`js/undertale_battle.js#L11-L23`
- 方案：URL query 解析，命中后在 hit 检测里跳过 HP 扣减 + defeat 分支
- 优点：简单直接，原生 Canvas 2D 渲染，好控制
- 状态：✅ 稳定可用

### 第二版：Sans 独立 BOSS 战页（定时器轮询）
- 路径：`sans/index.html`
- 方案：启动后 setTimeout 等 runtime 就绪，拿到 `all_global_vars`，用 setInterval 每 80ms 把 HP/KR 恢复到 MaxHP/0
- **问题**：C2 事件表在同一帧内就能跑完"Subtract HP → 判断 HP<=0 → 触发 defeat layout"三连，80ms 定时器根本插不进去，用户挨打后照样进 GameOver
- 提交：`b08115d`

### 第三版：hook setValue + Object.defineProperty（最终版）
- 方案：直接 monkey-patch `HP.setValue` 方法，让它无论传什么参数都调用 `_origSetValue(_godMax)`；同时 `Object.defineProperty(hp, 'data', {get: () => _godMax, set: () => {}})` 双重拦截（因为 C2 代码既有调用 `setValue` 也有直接读写 `data` 属性）
- KR 同理：`setValue = () => _origSetValue(0)` + `data` getter 锁死 0
- **本地验证**：`hp.setValue(0)` → `hp.data` 还是 92 ✓；直接 `hp.data = 1` → 读出来还是 92 ✓；`kr.setValue(999)` → 读出来还是 0 ✓
- 提交：`be3f082`

### 为什么之前不生效？
C2 事件表的"扣血→判死→切 layout"链在**同一个 tick 内同步执行**，没有任何异步间隙。定时器轮询本质是"事后擦屁股"，擦不到。必须**在写入那一瞬间就拦下来**。

## 📱 杉斯模拟器合集 APK 分析

用户上传了一份 APK（`杉斯模拟器合集.zip`），解压后发现是 **Lua 写的 WebView 壳子浏览器**，侧滑栏硬编码了 5 款线上 Sans 模拟器：

| # | 名字 | 地址 |
|---|------|------|
| 1 | 旧版困难 sans | `fuck-time.github.io/CJ-bad-time-simulator/` |
| 2 | 旧版困难 sans **锁血** | `caijiQAQ.github.io/CJ-s-bad-time-simulator–debug/` |
| 3 | fell sans **锁血** | `caijiQAQ.github.io/UNDERFELL-DEBUG/` |
| 4 | 蓝莓 **锁血** | `caijiqaq.github.io/UNDERSWAP-SANS-FIGHT-DEBUG/` |
| 5 | 尘埃 **锁血** | `caijiqaq.github.io/dusttale-debug/` |

## 📊 竞品资源体积对比（dusttale-debug vs 我们）

| 指标 | 我们的 Sans | caijiqaq 尘埃 debug | 差距 |
|------|------------|-------------------|------|
| **总大小** | **35 MB** | **1.0 MB** | **35×** |
| PNG 图片 | 131 张 · **16 MB** | 64 张 · **544 KB** | **30×** |
| OGG 音频 | 77 个 · **14 MB** | **0 个**（全是 404 占位） | ∞ |
| c2runtime.js | 1.1 MB | 743 KB | 1.5× |
| data.js | 814 KB | 244 KB | 3.3× |
| 文件数 | 340 | 82 | 4× |

### 人家加载快的 4 个核心原因

**① 音频全砍了（省 14 MB）**
人家 data.js 里还引用着 18 个 `.ogg`，但 GitHub Pages 上根本没有这些文件，全是 404 占位。C2 loader 对 404 会静默跳过，所以静音跑游戏。

**② 图片极度精简（省 15.5 MB）**
- `forestbg-sheet0.png`：我们 5.9 MB → 人家 `background-sheet0.png` 仅 9 KB
- Sans 相关 sprite sheet：人家每张都是 4-6 KB（128×128 极小尺寸）
- 我们有很多大尺寸 GIF 序列帧（`menu_gif-sheet{0,1,2}.png` 共 4.4 MB），人家直接砍了

**③ 事件表瘦了 3 倍（data.js 814 KB → 244 KB）**
人家砍掉了：Loader layout 进度条逻辑、所有 `nodamage` 保护事件、多个 Menu 子事件组、Phase 管理事件。

**④ 没有 Loader Layout**
人家 `MainMenu` 直接进 BattleScreen，省了 layout 切换开销。

### 资源优化建议（暂不实施，供后续参考）

| 优化 | 可省 | 风险 |
|------|------|------|
| 砍 data.js 里的 OGG 引用（静音） | -14 MB | 游戏静音，C2 loader 跳过不阻塞 |
| `pngquant`/`tinypng` 压缩 PNG | -10~12 MB | 可能降质，但像素图影响小 |
| 砍没用的事件组 | -500 KB | 容易搞坏 game flow |

当前总加载 35 MB，GitHub Pages CDN 传输确实慢（data.js 814 KB 需要 20 秒），但用户已表示"这个游戏已经很好了"，所以暂不优化，留给第二 Boss 做。

## 🚀 第二 Boss 预备

- 积分不足，先存日志
- 第二 Boss 具体方案待定
- 考虑复用 Debug 模式架构（`?debug=god` hook 全局变量）
- 资源加载慢的问题可在第二 Boss 上实施压缩优化（砍音频引用 + 压缩 PNG）

## 📝 提交记录

| Commit | 说明 |
|--------|------|
| `be3f082` | fix(sans): hook EventVariable.setValue instead of polling for debug god mode |
| `b08115d` | feat(sans): inject C2 runtime debug modes via ?debug=god\|sanshp\|all |
| `810f973` | feat(boss): add battle debug modes via URL query（TNO UndertaleBattle） |
| `9ae5e28` | fix: correct event tag priority table（military/story 排序） |
| `296d8c6` | fix: rename 德衫 to dustsans in title + loader |
