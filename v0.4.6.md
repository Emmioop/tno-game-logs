# v0.4.6 更新日志 · 事件密度重构 + 世界观改编 + 三极核制衡

> 发布日期：2026-08-04
> 版本号：SAVE_VERSION 19 · game.js?v=54 · ui.js?v=91 · style.css?v=84 · nation_sim.js?v=41 · save_system.js?v=7
> AI 助手：Trae

## 🎯 核心改动

### 1. 事件密度三重重构（解决测试员反馈"事件太多忽略玩法"）

#### 1.1 回合模式事件预算配置
测试员反馈：精简版弹窗太多，沉浸版虽长但也不要过载。

新增 `TURN_MODE_CONFIG` 双轨配置：

| 参数 | 精简版(quarterly, 156回合) | 沉浸版(bimonthly, 936回合) |
|------|---------------------------|---------------------------|
| coreBudget | 1（每回合最多1弹窗） | 2（每回合最多2弹窗） |
| quietChance | 45% 空白回合 | 25% 空白回合 |
| randomChanceMod | 0（无随机事件） | 0.15（×0.15概率） |
| randomMinInterval | 999（无随机事件） | 10（间隔10回合） |
| demoteMinor | true（minor降级风味） | false（保留） |
| demoteMajor | true（major降级风味） | false（保留） |
| criticalSparseRate | 0.85（critical 85%弹窗） | 1.0（全部弹窗） |
| storySparseRate | 0.90（story 90%弹窗） | 1.0（全部弹窗） |
| blockQuietTags | 仅critical阻止空白 | critical+story阻止空白 |

**预估弹窗数**：
- 精简版：约 80 个（平均每2回合一个），剧情+玩法平衡
- 沉浸版：约 240 个（平均每4回合一个），非常完整

#### 1.2 事件筛选算法升级
- **优先级排序**：critical(0) → story(1) → major(2) → diplomacy(3) → economy(4) → military(5) → minor(6) → 未知(7)
- **coreBudget 限制**：超出预算的事件加入 `pendingQueue` 推迟到下一回合
- **criticalSparseRate / storySparseRate 稀疏化**：用事件ID哈希稳定降级部分事件为风味，不丢失数据
- **blockQuietTags**：按 tag 级别控制空白回合，不是"有任何事件就阻止空白"

#### 1.3 16组重复事件ID清理
发现 **268个事件中有16组重复ID**，`once: true` 导致只触发第一个，后续全部被吞。修复方式：
- 不同阶段的事件分配新ID（如 `ev_bormann_honeymoon_stagnation` vs `ev_bormann_stagnation`）
- 完全重复的删除多余（如冷战的终结、新世界秩序）
- 同主题不同角度分配新ID（如 `ev_mars_program_start` vs `ev_mars_race_final`）

### 2. 时间线顺序修正（根本Bug：先建保护区才内战）

#### 2.1 tagPriority 排序 Bug 修复
```javascript
// 错误代码：critical优先级=0被当成falsy处理
const ta = tagPriority[a.tag] || 7;  // 0 || 7 = 7 ❌

// 修复后
const ta = (a.tag in tagPriority) ? tagPriority[a.tag] : 7;
```
此bug导致 `critical` 事件被排到最后，内战事件反而排在保护区后面。

#### 2.2 53个关键事件新增显式 priority 字段
同 tag 内按 priority 排序，确保顺序正确。

#### 2.3 修正后的关键时间线
```
1962Q1  贝格霍夫的枪声（希特勒遇刺）
1963Q3  恶魔之死（希特勒去世）
1963Q4  选择你的元首 + 达拉斯枪声(肯尼迪遇刺)
1964Q1  兄弟相残(内战爆发) → 斯派达尔中立区 → 日耳曼尼亚保护区 → 霍费尔割据
1964Q2  霍费尔正式割据 + 勃艮第阴影
1964Q4  日耳曼尼亚之战（内战高潮）
1965Q2  新元首（内战结束）
1972Q1  俄罗斯统一战争
1975Q3  大崩溃
1981Q2  AA线危机
1991Q1  冷战终结
2000Q1  终章·千年之后
```

### 3. 事件分类修复（德国事件误判为日本/美国）

#### 3.1 classifyDirection 仅匹配 ID+标题
**问题**：之前匹配 body 文本，事件描述中偶尔提外国名导致误分类（如德国事件中提到"日本刺客"被分类为日本方向）

**修复**：`classifyDirection` 和 `classifyEvent` 都只匹配 `(ev.id || '') + ' ' + (ev.title || '')`，不匹配 body。

#### 3.2 清理宽泛关键词
- 删除 `democracy`、`republic`、`president` 等易误匹配关键词
- 新增事件ID前缀匹配：`ev_japan_`、`ev_us_`、`ev_russia_`、`ev_ofn_`

#### 3.3 徽章从图标改为文字标签
`🇩🇪🇯🇵🇺🇸🇷🇺` 等图标 → `[德国内政]`、`[日本·共荣圈]`、`[美国·OFN]`、`[俄罗斯]`、`[国际]`，更清晰不歧义。

### 4. 开始页面改版——明确标注"TNO个人改编世界观"

#### 4.1 顶部三级声明
```
标题副标：架空历史 · 1962 — 2000
标题标签：基于 TNO 世界观的个人改编 · 大日耳曼国策略推演
红框徽章：⚡ 本作并非原版 TNO，二战结局与历史细节已大幅改写
```

#### 4.2 三大分歧点突出显示
序章开头直接点明"这不是你所熟知的 TNO 时间线"，用红色边框+金色◆标记的特殊区块展示与原版TNO的关键差异：
- ◆ **1940 · 敦刻尔克的陷落**——英军主力折损殆尽
- ◆ **1941 · 莫斯科的冬天没有来**——寒冬推迟一个月，斯大林撤离途中病逝
- ◆ **1944 · 海森堡没有算错**——正确测算铀临界质量，德国先研发出原子弹

#### 4.3 世界观声明区扩展
从简单声明扩展为详细说明：
- 明确写明"个人改编创作，并非原版 TNO"
- 列出保留的核心框架（三极冷战、继承危机、勃艮第威胁、俄罗斯军阀割据）
- 列出重写的部分（二战结局、历史细节）

### 5. 初始领导人修正

对照1962年开局时间线修正4处错误：

| 国家 | 旧值 | 新值 | 修正理由 |
|------|------|------|----------|
| 德国 | 阿登纳 | 希特勒(垂死) | 1962年1月遇刺，1963年10月去世，仍在任 |
| 美国 | 尼克松 | 肯尼迪 | 肯尼迪1960-1963在任，尼克松1969才当选 |
| 意大利 | 齐亚诺 | 墨索里尼 | 1962年墨索里尼活着，齐亚诺是后期演变 |
| 勃艮第 | 海德里希 | 希姆莱 | 希姆莱就是勃艮第之主 |

修正范围：`nation_sim.js` DEFAULTS + 6个JSON文件 + `ui.js` 世界地图标签。

### 6. 三极核力量重构（与世界观对齐）

用户反馈：日本不可能依赖德国核保护（二战后已闹掰）；德国核武应多于美国才能制衡；日本也需有核武才能三极冷战。

#### 修正后的核力量对比（1962年开局）

| 国家 | 旧弹头数 | 新弹头数 | 投送能力 | 核威慑 |
|------|----------|----------|----------|--------|
| 德国 | 2 | **580** | 60 ICBM + 16 SLBM + 220轰炸机 | 78 |
| 美国 | 6 | **420** | 90 ICBM + 40 SLBM + 180轰炸机 | 72 |
| 日本 | 0 | **320** | 40 ICBM + 24 SLBM + 120轰炸机 | 68 |
| 勃艮第 | 1 | 36 | 8 ICBM + 24轰炸机 | 45 |
| 意大利 | 0 | 0 | — | — |
| 俄罗斯 | 0 | 0 | — | — |

#### 逻辑自洽
- **德国 > 美国 > 日本**，三方都有核武形成制衡
- 德美差距不大（580 vs 420），解释为何德国用核威慑而非核优势
- 美国投送能力较强（90 ICBM + 40 SLBM）但弹头少，体现被特工破坏推迟3年的影响
- 日本 nuclearTier: 0 → 1，独立核武计划，与德国决裂后自研
- 日本"核武器由军部最高会议统管，海陆均无独立发射权"设定依然成立

同步修正：
- `game.js` initial resources: nukes 2→580, nukeDeter 30→78, deterrence 60→125
- `nation_sim.js` 6国 nuclear 数据
- JSON 数据文件同步

### 7. 时代风貌Tab修复（滑动不了）

**问题**：`#tab-flavor` 元素缺少滚动样式，内容超出无法滑动。

**修复**（style.css）：
```css
#tab-flavor {
  flex: 1;
  overflow-y: auto;
  min-height: 0;
  padding: 16px 20px 40px;
}
```

### 8. 启动页选择视觉反馈增强

**问题**：点击开始页面的按钮后不明显。

**增强**：
- 选中按钮：2px 金色边框 `var(--accent-gold-bright)` + 左侧 3px 金色色条
- 右上角 ✓ 标记
- 悬停：`background: rgba(201, 148, 68, 0.08)` + 边框发光
- 非选中：灰色边框，点击后视觉变化明显

### 9. 开发者模式升级

新增功能：
- **状态信息栏**：当前回合数/总数、剩余寿命、路线进度、关键flag一览
- **路线切换**：一键切换改革/保守/军国/党卫军四条路线
- **外交控制**：直接调整与各国的关系值
- **UI分区优化**：按钮分区域排布，样式从红色改为金色系

### 10. 世界观 v2.1 文档创作

完成 544 行个人改编世界观文档，核心框架：
- **二战修订**：敦刻尔克陷落→莫斯科冬天没来→斯大林病逝→海森堡没算错→雷克雅未克停战→第二次海狮计划→英国陷落
- **势力格局**：大日耳曼国（腐朽霸主）vs 共荣圈（三足金乌）vs OFN（蒙尘灯塔）+ 三头同盟（第四极）+ 俄罗斯军阀（破碎巨熊）
- **继承危机**：四头秃鹫（施佩尔/鲍曼/戈林/海德里希）+ 斯派达尔保护区 + 霍费尔割据 + 希姆莱末日计划
- **时间线纲要**：1962-1970内战与重建 → 1970-1980大崩溃与改革 → 1980-1990 AA线与冷战动摇 → 1990-2000终章
- 文档位置：`/workspace/tno-game-logs/世界观_v2.1.md`

## 📊 效果对比

| 指标 | v0.4.5 | v0.4.6 |
|------|--------|--------|
| 精简版弹窗(156回合) | ~76 | **~80**（剧情+玩法平衡） |
| 沉浸版弹窗(936回合) | ~148 | **~240**（非常完整） |
| 德国开局核弹 | 2 | **580** |
| 美国开局核弹 | 6 | **420** |
| 日本开局核弹 | 0 | **320** |
| 事件分类准确率 | 误判多（匹配body） | **仅ID+标题，极少误判** |
| 内战→保护区顺序 | ❌ 保护区在前 | ✅ 内战爆发→建立中立区→保护区 |
| 重复ID事件 | 16组（只触发第一个） | **0组**（全部重命名/删除） |
| 世界观声明 | 简单免责 | **三级声明+三大分歧点** |
| 初始领导人正确性 | 4处错误 | **全部正确** |
| 时代风貌Tab滚动 | ❌ 无法滑动 | ✅ 正常滚动 |

## 🔧 技术细节

### game.js 改动
- `SAVE_VERSION` 18 → 19
- 新增 `TURN_MODE_CONFIG` 双轨配置（quarterly/bimonthly）
- `getEventsForTurn()` 完全重写：优先级排序 → budget限制 → sparse稀疏化 → demote降级 → pendingQueue推迟
- `classifyDirection()` / `classifyEvent()` 只匹配 ID+标题，不匹配 body
- tagPriority 排序：`|| 7` → `(in tagPriority) ? :` 修复 0 值误判
- 53个关键事件新增 `priority` 字段
- 16组重复ID事件分配新ID或删除
- initial resources: nukes 2→580, nukeDeter 30→78, deterrence 60→125

### ui.js 改动
- 方向徽章：图标 → 文字标签（`[德国内政]`、`[日本·共荣圈]` 等）
- `renderNextTurnButton()` 回合模式按钮文字切换
- 世界地图标签领导人修正
- 开发者模式扩展：状态栏、路线切换、外交控制
- 启动页选择：选中样式、✓标记、悬停效果

### nation_sim.js 改动
- 6国 DEFAULTS: leader/capital/ideology 补全
- 6国 DEFAULTS nuclear 数据重构（德580/美420/日320/勃36）
- 日本 nuclearTier: 0 → 1

### style.css 改动
- `#tab-flavor` 新增滚动样式（flex:1 + overflow-y:auto）
- `.diff-btn.selected` 金色边框 + 左侧色条 + ✓标记
- `.splash-badge` 世界观改编徽章样式（红框+金字）
- `.pl-divergence` 三大分歧点特殊列表样式（红色背景+金色◆标记）
- 开发者模式按钮样式（分区、金色系）

### index.html 改动
- 标题副标：`TNO` → `架空历史`
- 标题标签：明确写"基于 TNO 世界观的个人改编"
- 新增 `.splash-badge` 红框徽章
- 序章改写：开头点明非原版TNO + 三大分歧点突出区块
- 七、三头同盟：意大利领导人从"齐亚诺"改回"墨索里尼"
- 免责声明扩展为详细世界观声明
- 版本号递增：style.css?v=83→84, game.js?v=53→54, ui.js?v=90→91

### data/countries/*.json 改动
- GER.json / USA.json / JAP.json / BUR.json / ITA.json / RUS.json: leader+capital+ideology补全
- GER.json / USA.json / JAP.json / BUR.json: nuclear 数据同步修正
- JAP.json: tech.nuclearTier 0→1

## ⚠️ 升级注意

1. **存档不兼容**：SAVE_VERSION 从 18 → 19，旧存档自动重置开局（不删除只重置提示）
2. **浏览器缓存**：GitHub Pages 可能缓存旧版 JS/CSS，打开后按 Ctrl+Shift+R 强制刷新
3. **事件顺序**：重复ID事件清理后，部分后期事件的ID已变更（如火星计划分阶段），如果有依赖旧ID的自定义逻辑需要更新
4. **存档读取**：turnMode/halfMonth/flavorLog 等字段兼容旧存档

## 📋 后续优化方向（非本轮）

- [ ] 事件过滤器面板：允许玩家筛选特定方向/标签的事件
- [ ] 风味事件详情查看：时代风貌Tab中点击可查看完整body和选项
- [ ] 核战争模拟：基于核弹头数量和投送能力的终末推演
- [ ] 存档版本迁移脚本：从18→19自动转换而非直接重置
