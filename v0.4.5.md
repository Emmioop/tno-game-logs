# v0.4.5 更新日志 · Bug修复 + 空白回合 + 随机事件精简 + 半月模式

> 发布日期：2026-08-04
> 版本号：SAVE_VERSION 18 · game.js?v=49 · ui.js?v=85 · style.css?v=80 · save_system.js?v=5
> AI 助手：Trae

## 🎯 核心改动

### 1. 严重 Bug 修复：科技研发完全失效
**问题**：`researchTech()` 方法在 game.js 中被定义了两次，第二个定义（旧版）覆盖了第一个（新版），导致：
- 新科技树（military/civil/nuclear/rocket）研发完全失效，点击返回"科技不存在"
- 旧科技研发不再触发 `addNews` 和 `clampResources`
**修复**：删除第二个重复定义，保留新版路由逻辑。

### 2. 删除无用文件和代码
- 删除根目录旧版重复文件：`game.js`、`ui.js`、`save_system.js`、`style.css`（未被 index.html 引用，内含旧格式 bug）
- 删除 `js/canvas_map.js`（从未被加载，实际使用 svg_map.js）
- 修复 `FCTIONS` → `FACTIONS` 拼写错误（潜在隐患）

### 3. 空白回合机制
测试员反馈事件太多。新增"平静回合"机制：
- 当回合没有 critical/major/story 级核心事件时，约 40% 的回合为"平静回合"
- 平静回合中所有事件自动结算，不弹窗
- 使用伪随机 `(turn * 7 + 13) % 10 < 4` 确保分布均匀
- **效果**：3回合测试中，弹窗从 8-12 个降至 5 个（含初始事件）

### 4. 随机事件大幅减少
**问题**：随机事件触发概率过高（普通25%/困难40%/地狱55%），156回合中触发约 40-90 个随机事件。
**改进**：
- 触发概率减半：`crisisChance * 0.5`（普通12.5%/困难20%/地狱27.5%）
- 最小间隔 3 回合：`turnsSinceRandom >= 3`
- **效果**：156回合中随机事件从 40-90 个降至约 10-20 个

### 5. 半月回合模式
新增"半月模式"，每回合 = 半个月：
- 季度模式（原版）：156 回合，每回合 = 1 季度
- 半月模式（新增）：936 回合，每回合 = 半个月（上旬/下旬）
- 日期显示：`1962年 第一季度上旬` / `1962年 第一季度下旬`
- 按钮文字自动切换：`推进至下一季度 ▸` ↔ `推进至下半月 ▸`
- 剧情事件仍按季度匹配（上下旬都触发同一季度事件）
- 存档兼容：`turnMode` / `halfMonth` 字段，旧存档自动补 `quarterly` / `0`

## 📊 效果对比

| 指标 | v0.4.4 | v0.4.5 |
|------|---------|---------|
| 科技研发 | ❌ 完全失效 | ✅ 正常工作 |
| 每回合弹窗数 | 4-8 个 | **0-4 个**（含空白回合） |
| 随机事件(156回合) | 40-90 个 | **10-20 个** |
| 回合模式 | 仅季度(156) | **季度(156) + 半月(936)** |
| 无用文件 | 5个旧文件+canvas_map | **已清理** |

## 🔧 技术细节

### game.js 改动
- `SAVE_VERSION` 17 → 18
- 新增 `turnMode: 'quarterly'` 属性
- 删除第二个 `researchTech()` 定义（行 824-837）
- 修复 `FCTIONS` → `FACTIONS` 拼写
- `init()` 新增 `halfMonth: 0`、`turnMode`、`totalTurns` 动态计算
- `getDateStr()` 半月模式显示"上旬/下旬"
- `turnToYearQuarter()` 半月模式每8回合=1年
- `matchesTurn()` 支持半月的 `halfMonth` 匹配
- `advanceTurn()` 时间推进分支：半月模式 halfMonth++ → quarter++
- `checkEnding()` 半月模式需到 Q4 下旬
- `getEventsForTurn()` 随机事件概率减半 + 3回合间隔 + 空白回合机制

### ui.js 改动
- 按钮文字根据 `turnMode` 动态切换（5处）
- `renderTab()` / `showNextEvent()` 恢复按钮文字适配半月

### save_system.js 改动
- `deserialize()` 兼容 `turnMode` / `halfMonth` / `flavorLog`

### index.html 改动
- 新增回合模式选择 UI（季度/半月）
- 回合模式选择初始化脚本
- 版本号更新

## ⚠️ 升级注意
- SAVE_VERSION 升至 18，**旧存档不兼容**
- 半月模式是可选的，默认仍为季度模式
