# v0.4.4 更新日志 · 事件分类系统实现

> 发布日期：2026-08-04
> 版本号：SAVE_VERSION 17 · game.js?v=48 · ui.js?v=84 · style.css?v=79 · save_system.js?v=4
> AI 助手：Trae

## 🎯 核心改动

### 背景
测试员反馈每回合弹窗事件过多（8-12个），登月等不影响大局的事件与政治抉择混在一起，体验混乱。本次更新将事件系统重构为双轨制：风味事件自动结算 + 核心政治事件弹窗，并对核心事件按方向分类。

### 1. 事件双轨制：风味 vs 核心

**风味事件**（不影响帝国走向）不再弹窗，自动结算后归档到「📰 时代风貌」Tab：
- 太空竞赛：登月、火星着陆、空间站
- 科技革命：计算机、互联网、基因工程、数字革命
- 环境变迁：亚特兰特罗帕、地中海干涸、核事故
- 社会文化：人口寒冬、千禧年、亚文化、瘟疫
- 玩家随时可翻阅风貌Tab，了解时代切片

**核心政治事件**保留弹窗，玩家亲手抉择：
- 每回合最多弹窗 4 个（`EVENT_CONFIG.coreBudget`）
- 超出预算的核心事件降级为风味事件自动结算
- 按 tag 优先级排序（critical > major > story > diplomacy > ...）

### 2. 核心事件方向分类 + 彩色徽章

每个核心事件弹窗和事件日志显示方向徽章：

| 方向 | 图标 | 颜色 | 涵盖内容 |
|------|------|------|----------|
| 🏛️ 国内 | 金黄 | `#c9a84a` | 继承、内战、经济、奴隶、党卫军、勃艮第、中立区 |
| ⛩️ 对日 | 粉红 | `#e87a9e` | 日本、共荣圈、中国、朝鲜、满洲 |
| 🦅 对美 | 蓝色 | `#5a8af2` | OFN、美国、民权、核裁军、缓和 |
| 🪖 对俄 | 红色 | `#e85040` | 俄罗斯统一、军阀、西俄战争、乌拉尔 |
| 🌐 其他 | 灰色 | `#7a7a8a` | 地中海、意大利、伊比利亚、土耳其、法国、非洲 |

### 3. 事件日志增强
- 事件日志每条增加方向色条（左侧 3px 彩色边框）
- 事件弹窗标题区显示方向徽章
- 时代风貌 Tab 卡片同样带方向色条和徽章

## 📊 分类统计（274个剧情事件）

| 类别 | 数量 | 说明 |
|------|------|------|
| 风味事件 | 42 | 自动结算，归入风貌Tab |
| 核心·国内 | 40 | 弹窗，金色徽章 |
| 核心·对日 | 49 | 弹窗，粉色徽章 |
| 核心·对美 | 37 | 弹窗，蓝色徽章 |
| 核心·对俄 | 77 | 弹窗，红色徽章 |
| 核心·其他 | 29 | 弹窗，灰色徽章 |

## 📊 效果对比

| 指标 | v0.4.1 及之前 | v0.4.4 |
|------|----------------|---------|
| 每回合弹窗数 | 8-12 个 | **最多 4 个** |
| 风味事件触达率 | 0%（淹没在弹窗海） | **100%**（Tab 可翻阅） |
| 事件方向识别 | 无 | **5 类彩色徽章** |
| 事件日志可读性 | 纯文本列表 | **方向色条 + 徽章** |

## 🔧 技术细节

### game.js 改动
- `SAVE_VERSION` 16 → 17
- 新增 `EVENT_CONFIG`：`{ coreBudget: 4, autoArchiveFlavor: true }`
- 新增 `FLAVOR_KEYWORDS` 数组：42个关键词匹配风味事件（登月/太空/科技/环境/社会等）
- 新增 `DIRECTION_KEYWORDS` 对象：4个方向的关键词列表（japan/us/russia/other）
- 新增 `classifyEvent(ev)`：判断风味/核心，**只匹配ID和标题**（避免body误匹配）
- 新增 `classifyDirection(ev)`：判断方向（russia > japan > us > other > internal 优先级）
- 新增 `autoResolveFlavorEvent(ev)`：风味事件自动选取第一个可用选项结算，记入 `state.flavorLog`
- 重写 `getEventsForTurn()`：返回 `{core, flavor}` 对象，核心事件按tag优先级排序+预算上限
- `advanceTurn()` 适配新格式：风味事件自动结算，核心事件交给UI弹窗
- `chooseEventOption()` 日志新增 `direction` 字段
- `state` 初始化新增 `flavorLog: []`

### ui.js 改动
- `renderTab()` 新增 `flavor` 分支：控制双容器（`#tab-content` / `#tab-flavor`）显隐
- 新增 `renderFlavorLog()`：渲染时代风貌Tab，含空状态引导文案
- `renderEventLog()` 重写：每条事件带方向徽章和色条
- `showEventModal()` 重写：弹窗标题区显示方向徽章，tag文本支持更多类型
- `showNextEvent()` 完成后刷新风貌Tab（如果正在显示）
- `processTurnEvents()` 适配新格式：风味事件自动结算，核心事件弹窗

### style.css 改动
- 新增 5 种 `.dir-badge` 方向徽章样式（金黄/粉红/蓝/红/灰）
- 新增 5 种 `.event-card.dir-*` / `.flavor-card.dir-*` 方向色条
- 新增 `.flavor-header` / `.flavor-list` / `.flavor-card` / `.flavor-date` / `.flavor-title` / `.flavor-text` 样式
- 新增 `.empty-hint` 空状态提示样式

### save_system.js 改动
- `deserialize()` 增加 `flavorLog` 字段兜底（旧存档自动补 `[]`）

### index.html 改动
- 版本号升级：game.js?v=48, ui.js?v=84, style.css?v=79, save_system.js?v=4
- 「📰 时代风貌」Tab 已在 v0.4.2 添加

## 🔍 分类逻辑要点

1. **风味事件判断只匹配 ID 和标题**：早期版本匹配 body 导致"网络""计算机"等词在 body 中出现时误分类（如"鲍曼的党机器"body含"网络"被误归风味）。修正后只匹配 ID+标题。
2. **方向分类匹配所有文本字段**：title + desc + body + text + content + id，确保覆盖面。
3. **优先级**：russia > japan > us > other > internal(默认)。因为很多俄罗斯事件可能同时涉及外交。
4. **显式覆盖**：事件可设置 `ev.category = 'flavor'` 或 `ev.direction = 'russia'` 来覆盖自动分类。

## ⚠️ 升级注意
- SAVE_VERSION 升至 17，**旧存档（v16 及以下）会自动清除**，需要开新档
- 这是预期行为——风味事件系统改变了事件流结构，旧存档不兼容

## 📝 后续计划
- [ ] 事件过滤器面板（玩家可勾选"只看对俄事件"等）
- [ ] 风味事件详情展开（点击卡片看完整描述）
- [ ] 事件搜索功能
- [ ] 按方向分组的折叠视图
