# v0.4.2 更新日志 · 事件分类与精简系统

> 发布日期：2026-08-03  
> 版本号：SAVE_VERSION 17 · game.js?v=47 · ui.js?v=83 · style.css?v=78

## 🎯 核心改动

### 1. 事件双轨制：核心 vs 风味
- **风味事件**（登月、奥运、圣诞、阅兵、流行病、流行文化等）不再弹窗
- 风味事件自动结算效果，归档到新增的「📰 时代风貌」Tab
- 玩家随时可翻阅，了解那个时代的风貌，不再被强制点击

### 2. 事件方向分类 + 彩色徽章
每个核心事件标题前显示方向徽章：
- 🏛️ **金黄** = 国内（希特勒、党卫军、经济、奴隶、核武、勃艮第）
- ⛩️ **粉红** = 对日（日本、共荣圈、三头同盟）
- 🦅 **蓝色** = 对美（OFN、阿留申、民权、核谈）
- 🪖 **红色** = 对俄（西俄、乌克兰、克格勃、西伯利亚）
- 🇮🇹 **绿色** = 对意/地中海（意大利、伊比利亚、土耳其）
- 🌐 **灰色** = 其他

### 3. 事件日志增强
- 事件日志每条增加方向色条（左侧 3px 彩色边框）
- 事件弹窗标题区显示方向徽章
- 时代风貌 Tab 卡片同样带方向色条

## 📊 效果对比

| 指标 | v0.4.1 及之前 | v0.4.2 |
|------|----------------|---------|
| 每回合弹窗数 | 8-12 个 | **最多 4 个** |
| 风味事件触达率 | 0%（淹没在弹窗海） | **100%**（Tab 可翻阅） |
| 事件方向识别 | 无 | **6 类彩色徽章** |
| 事件日志可读性 | 纯文本列表 | **方向色条 + 徽章** |

## 🔧 技术细节

### game.js 改动
- 新增 `classifyEvent()` → 判断是否为风味事件
- 新增 `classifyDirection()` → 判断事件政治方向（russia > japan > america > italy > internal 优先级）
- 新增 `autoResolveFlavorEvent()` → 风味事件静默结算
- 重写 `getEventsForTurn()` → 返回核心事件数组（已按优先级排序 + 预算上限 4）
- `SAVE_VERSION` 16 → 17
- 危机概率下调：normal 0.40→0.30, hard 0.55→0.45, hell 0.70→0.55
- 修复：russia 关键词数组重复行导致的语法错误

### ui.js 改动
- 重写 `showEventModal()` → 生成 HTML 时直接内嵌方向徽章（不再依赖脆弱的装饰器 DOM 查询）
- 重写 `renderEventLog()` → 直接生成带方向色条和徽章的 HTML
- 新增 `renderFlavorLog()` → 渲染时代风貌 Tab
- 重写 `renderTab()` → 添加 flavor 分支
- 重写 `showNextEvent()` → 完成后刷新风貌 Tab
- 修复 `nextTurn()` → 单一事件流，避免 Game.advanceTurn 和 processTurnEvents 双触发
- 新增 `onEvents()` / `onTurnEnd()` 回调

### save_system.js 改动
- `deserialize()` 增加 `flavorLog` 字段兜底（旧存档自动补 `[]`）

### style.css 改动
- 新增 6 种 `.badge-*` 徽章样式
- 新增 `.event-card.dir-*` / `.event-log-item.dir-*` 方向色条
- 新增 `.flavor-card` / `.flavor-header` / `.flavor-list` / `.flavor-date` / `.flavor-title` / `.flavor-text` 样式
- 新增 `.empty-hint` 空状态提示样式
- `.news-item.flavor` 风味新闻斜体半透明

### index.html 改动
- 新增「📰 时代风貌」Tab（第 119 行）
- 版本号升级：game.js?v=47, ui.js?v=83, style.css?v=78

## ✅ 测试结果

```
=== 功能测试 ===
  classifyDirection: 15/15 通过
  isFlavorEvent:    5/5  通过
  getEventsForTurn:  返回 Array ✅
  SAVE_VERSION:      17 ✅
  EVENT_CONFIG:      coreBudget=4, autoArchiveFlavor=true ✅
  关键词覆盖:        internal(70) japan(42) america(46) russia(41) italy(18) flavor(104)

=== 集成测试 ===
  UI 补丁函数:       10/10 存在
  徽章 HTML 输出:     6/6 正确
  flavorLog 初始化:   ✅
  autoResolveFlavor:  ✅ 稳定度 45→47
  旧存档兼容:         ✅ flavorLog 自动补 []
  语法检查:           game.js ✅ ui.js ✅ save_system.js ✅

=== 汇总: 20/20 通过 ===
```

## ⚠️ 升级注意
- SAVE_VERSION 升至 17，**旧存档（v16 及以下）会自动清除**，需要开新档
- 这是预期行为——风味事件系统改变了事件流结构，旧存档不兼容

## 📝 后续计划（v0.4.3+）
- [ ] 事件过滤器面板（玩家可勾选"只看对俄事件"等）
- [ ] 风味事件详情展开（点击卡片看完整描述）
- [ ] 事件搜索功能
- [ ] 按方向分组的折叠视图
