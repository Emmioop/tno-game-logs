# 千年帝国 + Dusttale Sans BOSS 战 · 技术实现完整手册

> **重要**：这是整个项目的技术总文档，涵盖所有模块实现细节、踩坑记录、Debug 用法、部署流程。
> **下个账号必读**：从这里开始，不用重新翻代码。
> 版本：v0.4.8 · 2026-08-09 · 对应主仓库 commit `be3f082`

---

# 一、项目架构总览

## 1.1 项目定位

两个独立但关联的游戏放在同一个 GitHub 仓库里：

| 子系统 | 技术栈 | 入口 | 说明 |
|--------|--------|------|------|
| **主游戏：千年帝国** | 纯原生 HTML/CSS/JS（无框架） | `/index.html` | TNO 风格政治模拟策略游戏，1962-2000 回合制 |
| **Sans BOSS 战** | Construct 2 运行时 + Canvas 2D 弹幕引擎 | `/sans/index.html` | Dusttale 同人 Undertale 风格 BOSS 战 |

## 1.2 目录结构

```
/workspace/tno-strategy-game/
├── index.html                 ← 主游戏入口
├── sans/index.html            ← Sans BOSS 战入口（C2 运行时）
├── js/
│   ├── game.js                ← 主游戏核心逻辑（国策、资源、回合推进、事件调度）
│   ├── events.js              ← 手写剧情事件
│   ├── events_gen.js          ← 自动生成的 80000 个随机事件（88MB）
│   ├── data.js                ← 静态数据（建筑、国策、科技树）
│   ├── save_system.js         ← LocalStorage 存档系统
│   ├── nation_sim.js          ← AI 国家模拟
│   ├── svg_map.js             ← SVG 矢量地图渲染
│   ├── ui.js                  ← UI 界面（国策面板、新闻、BOSS 战入口按钮）
│   ├── undertale_battle.js    ← TNO 内嵌版 Undertale 战斗引擎（纯 Canvas 手绘）
│   └── c2sf_engine.js         ← 弹幕引擎（抄原版 c2-sans-fight，CSV 指令集）
├── sans/
│   ├── c2runtime.js           ← Construct 2 运行时（1MB，导出后不可编辑）
│   ├── data.js                ← C2 导出的游戏数据（833KB，包含事件表、对象属性）
│   ├── MobileControls.js      ← 移动端虚拟摇杆 + Z/X/C 按钮（Canvas 2D 绘制）
│   ├── MobileGraphics/        ← C2 导出的精灵图（PNG）
│   ├── ds_attack*.csv         ← 弹幕轨道 CSV 文件（31 个）
│   ├── jquery-3.4.1.min.js    ← C2 依赖
│   ├── sw.js                  ← Service Worker（缓存版本 tno-dusttale-v3）
│   └── *.bak / sans_bak/      ← 历史备份，别删
├── assets/c2sf/               ← 弹幕引擎用的 sprite（骨头、龙骨炮、灵魂等）
├── data/                      ← TNO 世界数据（86MB）
├── img/                       ← 主游戏图片（26MB）
└── css/                       ← 主游戏样式
```

## 1.3 部署架构

```
GitHub repo (main)
    ↓  gh-pages 分支 = main 的快照（sans/ 目录完整复制过去）
    ↓  GitHub Pages
    ↓  https://emmioop.github.io/tno-strategy-game/
    ↓  用户浏览器 → Service Worker 首次缓存（sans/sw.js）
```

---

# 二、Sans BOSS 战（C2 版）核心实现

## 2.1 本质是什么？

Sans BOSS 战页面不是我们写的，是从**原版 c2-sans-fight** 下载的 Construct 2 导出项目，然后我们做了大量 hack：

- 下载地址：`https://github.com/Xayrn/c2-sans-fight`
- 原始导出：Construct 2 官方 runtime（`c2runtime.js`）+ 导出数据（`data.js`）+ 精灵图
- 我们的工作：**在不修改 C2 导出数据的前提下，通过外部脚本注入功能**

## 2.2 为什么不直接改 data.js？

- `data.js` 是 C2 编辑器导出的 JSON 数据，包含所有对象属性、事件表、布局
- 里面的全局变量定义、事件逻辑都是自动生成的，手动改极易搞坏
- C2 runtime 是闭包化的，内部函数外部访问不到（除非 monkey-patch）
- **核心策略**：所有 hack 都在 `index.html` 的 `<script>` 标签里注入，通过 `canvas.c2runtime` 访问运行时实例

## 2.3 index.html 里注入了哪些脚本？

按执行顺序：

```html
<!-- 1. jQuery（C2 依赖） -->
<script src="jquery-3.4.1.min.js?v=1786286012"></script>

<!-- 2. C2 运行时 -->
<script src="c2runtime.js?v=1786286017"></script>

<!-- 3. 移动端虚拟控件（我们写的） -->
<script src="MobileControls.js?v=20260809"></script>

<!-- 4. Debug 模式注入（hook EventVariable） -->
<script> ... _injectBattleDebug() ... </script>

<!-- 5. C2 启动 + Loader + Keyboard 注入 + Auto-Skip -->
<script>
  jQuery(document).ready(function() {
    cr_createRuntime("c2canvas");                    // 启动 C2 运行时
    _rt.enableWebGL = false; _rt.gl = null;          // 禁用 WebGL（强制 2D 渲染）
    _startLoader();                                   // 进度条 overlay
    _injectKeyboard();                                // 注入 Keyboard 插件实例
    _autoSkipToBattleScreen();                       // 自动跳过标题动画
  });
</script>
```

## 2.4 C2 Runtime 关键对象关系

```
document.getElementById('c2canvas').c2runtime
├── all_global_vars[]        ← 所有全局变量实例（HP, KR, MaxHP, EnemyHP...）
│   ├── .name                ← 变量名字（字符串）
│   ├── .data                ← 当前值（数字）
│   ├── .setValue(x)         ← 设值方法（可被 hook！）
│   └── ._godHooked          ← 我们加的标记（防止重复 hook）
├── layouts{}                ← 所有布局（MainMenu, BattleScreen, GameOver...）
├── running_layout           ← 当前运行的布局对象
├── isloading                ← 是否还在加载
├── loadingprogress          ← 0.0 ~ 1.0 加载进度
├── enableWebGL              ← WebGL 开关（我们强制设为 false）
└── __keyboardInstance       ← 我们注入的 Keyboard 插件实例
```

## 2.5 WebGL 禁用（关键修复！）

**问题**：原版 C2 项目默认启用 WebGL，但我们浏览器环境 WebGL 上下文创建总是失败，导致画面空白。

**修复位置**：`sans/index.html` 的 `cr_createRuntime` 之后立即：
```js
var _rt = document.getElementById("c2canvas").c2runtime;
if (_rt) { _rt.enableWebGL = false; _rt.gl = null; }
```

**备选方案**（如果上面的时机不对）：也可以直接改 `c2runtime.js` 第 4549 行附近：
```
this.enableWebGL = pm[13];   ← 改成 this.enableWebGL = false;
```

**原理**：C2 runtime 在第 3731 行附近会检查 `this.enableWebGL`，如果为 false 就走 `canvas.getContext("2d")` 的 2D 渲染路径。

## 2.6 Keyboard 插件注入（关键修复！）

**问题**：从 APK 提取的 C2 导出项目，`data.js` 里没有 Keyboard 插件的 Type 定义，导致 runtime 启动时 Keyboard 插件根本没实例化，键盘输入完全无效。

**修复位置**：`sans/index.html` 底部，`cr_createRuntime` 之后：

```js
(function() {
    var canvas = document.getElementById("c2canvas");
    var R = (canvas && canvas.c2runtime) || window.c2runtime;
    if (!R || !cr.plugins_.Keyboard) return;
    try {
        var plugin = new cr.plugins_.Keyboard(R);
        var type = new cr.plugins_.Keyboard.prototype.Type(plugin);
        type.name = "Keyboard";
        type.families = [];       // ← 必须显式设置！原版有，APK 里丢了
        type.behaviors = [];
        type.is_family = false;
        var inst = new cr.plugins_.Keyboard.prototype.Instance(type);
        inst.onCreate();          // ← 这步才是关键：注册 keydown/keyup 监听
        R.__keyboardInstance = inst;
    } catch(e) { console.warn("[keyboard] inject failed:", e); }
})();
```

**为什么 type.families 不能缺？** C2 的 Instance 构造函数内部会访问 `this.type.families`，如果是 undefined 就会报 `Cannot read properties of undefined (reading 'length')`。

## 2.7 Debug 模式完整实现原理（终极版）

### 问题根源

C2 事件表在**同一个 tick（帧）内**同步执行"扣血 → HP<=0 → 触发 defeat"三连。任何异步方案（setTimeout、setInterval、requestAnimationFrame）都赶不上，因为：

```
tick N:
  事件 A: Subtract HP by 30     ← 同步，HP 当场变成 0
  事件 B: Compare HP <= 0       ← 同步，条件成立
  事件 C: Go to layout "GameOver" ← 同步，当场切页面
  ← 没有任何异步间隙！定时器轮询在这里没用
```

### 解决方案：双重拦截 hook

**第一层：hook setValue**
```js
hp._origSetValue = hp.setValue.bind(hp);
hp.setValue = function (x) {
    return this._origSetValue(this._godMax);  // 无论传什么值，永远写 MaxHP
};
```

**第二层：Object.defineProperty 拦截 .data 属性**
```js
Object.defineProperty(hp, 'data', {
    get: function(){ return hp._godMax; },   // 任何读取都返回 MaxHP
    set: function(){ /* 忽略所有写入 */ },
    configurable: true,
    enumerable: true,
});
```

**为什么需要两层？** 因为 C2 事件表内部代码既有调用 `hp.setValue(x)` 也有直接赋值 `hp.data = x`，两种写法都要拦。

### 完整 hook 代码模板

```js
(function _injectBattleDebug() {
  var _up = new URLSearchParams(location.search);
  var d = _up.get('debug') || '';
  var DEBUG = {
    god:    d === 'god'    || d === 'all' || d === '1',
    sanshp: d === 'sanshp' || d === 'all',
    heal:   d === 'heal'   || d === 'all',
  };
  window.__C2_DEBUG__ = DEBUG;
  if (!DEBUG.god && !DEBUG.sanshp && !DEBUG.heal) return;

  function applyDebugs() {
    var canvas = document.getElementById('c2canvas');
    var R = canvas && canvas.c2runtime;
    if (!R || !R.all_global_vars) { setTimeout(applyDebugs, 200); return; }

    function gv(name) { return R.all_global_vars.find(function(v){ return v.name === name; }); }

    if (DEBUG.god) {
      var hp = gv('HP'), kr = gv('KR'), maxHp = gv('MaxHP');
      if (hp && !hp._godHooked) {
        var baseMax = maxHp ? maxHp.data : hp.data;
        hp._godHooked = true;
        hp._godMax = baseMax;
        hp._origSetValue = hp.setValue.bind(hp);
        hp.setValue = function (x) { return this._origSetValue(this._godMax); };
        Object.defineProperty(hp, 'data', {
          get: function(){ return hp._godMax; },
          set: function(){},
          configurable: true, enumerable: true,
        });
      }
      if (kr && !kr._godHooked) {
        kr._godHooked = true;
        kr._origSetValue = kr.setValue.bind(kr);
        kr.setValue = function(){ return kr._origSetValue(0); };
        Object.defineProperty(kr, 'data', {
          get: function(){ return 0; },
          set: function(){},
          configurable: true, enumerable: true,
        });
      }
    }
    if (DEBUG.sanshp) {
      var ehp = gv('EnemyHP'), ehm = gv('EnemyHpMax');
      if (ehp) { ehp.data = 1; }
      if (ehm) { ehm.data = 1; }
    }
  }
  jQuery(document).ready(function(){ setTimeout(applyDebugs, 100); });
})();
```

### URL 参数对照表

| URL | 效果 |
|-----|------|
| `?debug=god` | 无限血（HP 锁 MaxHP，KR 锁 0） |
| `?debug=sanshp` | Sans 只有 1 HP，一击必杀 |
| `?debug=heal` | 物品数量最大化 |
| `?debug=all` | 全开 |
| `?debug=1` | 等同 `god` |
| 无 `?debug=` | 正常游戏 |

### 运行时验证方法

在浏览器 Console 里：
```js
// 检查 hook 是否生效
document.getElementById('c2canvas').c2runtime.all_global_vars.find(v=>v.name==='HP')
// → 应该看到 _godHooked: true, _godMax: 92

// 手动测试
var hp = document.getElementById('c2canvas').c2runtime.all_global_vars.find(v=>v.name==='HP');
hp.setValue(0); hp.data;   // → 应该返回 92
hp.data = 1;    hp.data;   // → 应该还是 92
```

## 2.8 Loader 进度条实现

**文件位置**：`sans/index.html` 顶部 `<style>` 定义样式，底部 `<script>` 实现逻辑

**核心原理**：每 250ms 读取 `R.loadingprogress`（0~1）更新进度条宽度，同时根据百分比映射阶段文案：

```js
var stages = [
    [0,   "正在读取 data.js 项目数据"],
    [3,   "正在加载字体精灵"],
    [15,  "正在加载 Sans / Frisk 角色图"],
    [30,  "正在加载 Gaster Blaster 特效"],
    [50,  "正在加载弹幕 CSV 轨道"],
    [70,  "正在加载战斗音效"],
    [90,  "即将就绪，正在编译运行时"]
];
```

**卡住检测**：连续 5 秒百分比不变时提示"网络较慢，仍在加载…请耐心等待"

**完成检测**：`R.isloading === false` 或百分比 ≥ 100，然后 fade-out overlay

## 2.9 Auto-Skip 自动跳过标题动画

C2 导出的项目有 MainMenu → Loading → BattleScreen 的标题过场动画，每次要等好几秒。我们用 200ms 轮询检测当前布局，一旦不是 MainMenu 就直接切过去：

```js
var _skipInterval = setInterval(function() {
    var R = (document.getElementById("c2canvas").c2runtime);
    if (!R || !R.running_layout || R.isloading) return;
    if (R.running_layout.name === "BattleScreen") {
        clearInterval(_skipInterval); return;
    }
    var BL = R.layouts["BattleScreen"];
    if (BL) { R.changelayout = BL; }   // 直接设置 changelayout 属性，下一帧 C2 会自动切
}, 200);
```

**为什么设 `R.changelayout` 而不是直接调方法？** C2 runtime 内部每帧都会检查 `this.changelayout` 并切换布局。外部直接调私有方法可能有副作用。这是最干净的方式。

## 2.10 MobileControls.js 虚拟控件

**功能**：给移动端加一个全屏 Canvas overlay，画虚拟摇杆 + Z/X/C 按钮（Undertale 风格）

**文件**：`sans/MobileControls.js`（约 429 行，v2 重写版）

**布局**：
```
┌─────────────────────────────┐
│                         ┌─┐ │  Z 按钮（最右，青色 #00FFFF）
│                         └─┘ │
│                         ┌─┐ │  X 按钮（中间，橙色 #FFA040）
│                         └─┘ │
│                         ┌─┐ │  C 按钮（最左，绿色 #00A040）
│                         └─┘ │
│                             │
│  ╭───╮                      │
│  │ ○ │  ← 摇杆               │
│  ╰───╯                      │
└─────────────────────────────┘
```

**关键实现细节**：

1. **Canvas 创建**：全屏固定定位，`pointer-events: none`，这样 overlay 自己不拦截事件，但 `touchstart/touchmove/touchend` 通过 event bubbling 仍能触发（不对，其实需要开启 pointer events）—— 实际做法是 overlay canvas 设 `pointer-events:auto` 自己处理触摸，然后通过 `dispatchEvent` 模拟键盘事件

2. **游戏画面位置**：通过 `canvas.getBoundingClientRect()` 拿到 C2 canvas 在屏幕上的实际位置（因为 C2 用了 letterbox/contain 模式），然后按钮和摇杆基于这个 rect 定位

3. **触摸 → 键盘事件模拟**：
```js
// 模拟 keydown
var ev = new KeyboardEvent('keydown', { code: 'KeyZ', keyCode: 90, which: 90, bubbles: true });
document.dispatchEvent(ev);
```

4. **摇杆方向映射**：根据 stick 偏移向量计算方向，分别触发 ArrowUp/Down/Left/Right

**GAME_W vs 实际 canvas 尺寸**：MobileControls.js 里 `GAME_W = 960`，但 C2 canvas 实际是 640×480。这是因为 APK 里的项目分辨率是 960×540，我们改成了 640×480。MobileControls.js 做了 fallback：如果 `getBoundingClientRect()` 拿到 canvas 实际位置，就用实际值，否则才用 960×540 算。

## 2.11 Service Worker 缓存管理

**文件**：`sans/sw.js`

**缓存版本号**：`tno-dusttale-v3`（每次大改后 +1）

**清除旧缓存机制**：
```js
self.addEventListener('activate', e => {
  e.waitUntil(
    caches.keys().then(keys =>
      Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k)))
    ).then(() => self.clients.claim())
  );
});
```

**GitHub Pages 上实际禁用了 SW 注册**：
```js
// sans/index.html 里
window.C2_RegisterSW = function C2_RegisterSW() { return; } // disabled SW for GitHub Pages
```

**为什么禁用？** GitHub Pages 走 CDN，SW 会导致缓存更新不及时。但 SW 文件本身保留在仓库里，万一以后需要离线支持可以启用。

---

# 三、Sans BOSS 战（UndertaleBattle 内嵌版）实现

## 3.1 定位

主游戏 `/index.html` 内嵌的 BOSS 战模块，**独立的 Canvas 2D 实现**，不依赖 C2 runtime。用户在主游戏里点击"BOSS战"按钮会跳转到独立的 `sans/index.html`，但也留了一个纯 JS 版本（`undertale_battle.js`）供未来嵌入主游戏。

**文件**：`js/undertale_battle.js`（约 35KB，v120）

## 3.2 架构

```
undertale_battle.js（战斗逻辑 + UI 绘制）
    └── 依赖 c2sf_engine.js（弹幕引擎，抄原版 c2-sans-fight）
            ├── 解析 CSV 弹幕轨道文件（sans/assets/c2sf/ 或 sans/*.csv）
            ├── 每帧 update（更新子弹位置、检测碰撞）
            └── 每帧 draw（骨头、龙骨炮、灵魂、激光）
```

## 3.3 Debug 模式实现（比 C2 版简单）

因为是纯 JS，直接在 hit 检测里跳过：

```js
// undertale_battle.js 约第 912-930 行
const hit = C2SF.hitTest();
if (hit) {
    const dmg = 10;
    if (!DEBUG.god) state.player.hp -= dmg;   // ← 直接跳过扣血
    if (!DEBUG.god) {
        // KR 累积逻辑
    }
    C2SF.spawnDmg(cs.x, cs.y - 20,
        DEBUG.god ? '∞' : String(dmg),       // ← 命中飘字显示 ∞
        DEBUG.god ? '#44ff88' : '#ff4444');
}
if (!DEBUG.god && state.player.hp <= 0) {
    // defeat 逻辑
}
```

**URL 参数**：和 C2 版相同（`?debug=god&debug=sanshp&debug=all`）

**额外功能**：`DEBUG.phase`（空格键跳到下一个 phase）、`DEBUG.heal`（物品 ×99）

## 3.4 c2sf_engine.js 弹幕引擎

**来源**：完整抄自 `c2-sans-fight` 项目的 BHUTE 渲染引擎

**CSV 指令集**（`parseCSV` 函数实现）：

| 指令 | 说明 | 示例 |
|------|------|------|
| `bonev x y h vy` | 垂直骨头 | `bonev 320 226 165 -4` |
| `boneh x y w vx` | 水平骨头 | `boneh 100 300 24 3` |
| `blaster x y dir` | Gaster Blaster | `blaster 320 226 right` |
| `sine x y amp freq vy` | 正弦波移动骨头 | |
| `platform x y w h` | 蓝色重力平台 | |
| `stabs x y w h` | 骨头刺（带预警） | |
| `iv name start dur count interval` | 间隔发射 | |
| `wait ms` | 等待 | `wait 1000` |
| `goto label` | 跳转 | |

**Phase 列表**（21 个阶段）：

```
Intro → Multi 1 → Multi 2 → Multi 3 → PlatformBlaster →
Platforms 1-4 → BoneSlide V/H → BoneGap 1/1Fast/2 →
BlueBone → RandomBlaster 1/2 → BoneStab 1/2/3 → Final
```

每个 phase 有对应的 CSV 文件在 `sans/` 目录下。

---

# 四、主游戏（千年帝国）架构

## 4.1 技术栈

- 纯原生 HTML/CSS/JavaScript
- 零外部依赖（除了 Google Fonts）
- LocalStorage 存档
- GitHub Pages 部署

## 4.2 核心文件说明

| 文件 | 大小 | 职责 |
|------|------|------|
| `index.html` | 29KB | 入口，加载所有 JS，定义初始 DOM 结构 |
| `js/game.js` | 79KB | 游戏核心：回合推进、国策执行、事件调度、结局判定 |
| `js/events.js` | 409KB | 手写剧情事件（80+ 弹窗） |
| `js/events_gen.js` | **88MB** | 自动生成的随机事件（80000 个！） |
| `js/data.js` | 62KB | 静态数据结构（建筑、国策、科技树定义） |
| `js/ui.js` | 291KB | UI 渲染：国策面板、新闻滚动、事件弹窗、BOSS 按钮 |
| `js/save_system.js` | 8KB | LocalStorage 多槽位存档（5 手动 + 1 自动） |
| `js/svg_map.js` | 84KB | SVG 矢量地图渲染 |
| `js/nation_sim.js` | 48KB | AI 国家模拟（GDP/军事/科技/人口） |
| `js/datastore.js` | 15KB | 运行时数据存储 |

## 4.3 事件优先级系统

**位置**：`js/game.js` 约第 921-930 行

```js
const tagPriority = {
    critical: 0,    // 最高：核危机、世界大战、帝国崩塌
    major: 1,       // 重要：关键国策完成、外交转折
    military: 2,    // 军事：东线战事、戈林行动
    diplomacy: 3,   // 外交
    economy: 4,     // 经济、科技、文化
    tech: 4,
    culture: 4,
    story: 5,       // 叙事风味（不能压过军事/外交/经济！）
    minor: 6,       // 次要
    random: 8,      // 随机
};
```

**踩过的坑**：早期版本 `military=5, story=2`，导致叙事弹窗压过军事决策。修正后 `military` 升为 2，`story` 降为 5。

## 4.4 三模式系统

| 模式 | 国策速度 | 事件密度 | 初始资源 | 说明 |
|------|----------|----------|----------|------|
| 历史 | 1.0x | 完整（240 弹窗） | 默认 | 原版体验 |
| 沙盒 | 1.6x | 精简（80 弹窗） | +200 资金/50 研发 | 快速实验 |
| 混乱 | 1.3x | 精简 + 随机扰动 | ±30% 波动 | 每回合稳定度±6、外交±12、15% 随机事件 |

## 4.5 存档系统

**存储**：`localStorage.setItem('tno_save_slot_N', JSON.stringify(saveData))`

**结构**：
```js
{
    version: 1,
    slot: N,
    timestamp: 1723000000,
    year: 1964, quarter: 2,
    route: 'speer',         // 国策路线
    difficulty: 'normal',
    mode: 'historical',
    state: { /* 完整游戏状态深拷贝 */ }
}
```

**自动存档**：每回合结束保存到 slot 0

---

# 五、GitHub Pages 部署流程

## 5.1 两个分支

```
main        ← 开发分支，完整代码
gh-pages    ← 部署分支，= main 的快照
```

**为什么 gh-pages 要单独一个分支？** 因为 GitHub Pages 会把 gh-pages 分支的根目录作为网站根目录。而我们的 `sans/` 目录要能通过 `emmioop.github.io/tno-strategy-game/sans/index.html` 访问，所以 sans/ 必须在仓库根目录下。

## 5.2 同步流程

**开发后同步 gh-pages：**

```bash
cd /workspace/tno-strategy-game
git checkout main
git add -A && git commit -m "..."
git push origin main
git checkout gh-pages
git reset --hard main
git push origin gh-pages --force   # ← 关键！强制推送确保 gh-pages 完全等于 main
git checkout main
```

**快捷命令（一行同步）：**

```bash
cd /workspace/tno-strategy-game && git checkout gh-pages && git reset --hard main && git push origin gh-pages --force && git checkout main
```

## 5.3 缓存 Busting 策略

**问题**：GitHub Pages CDN 会缓存静态文件，用户看到的可能是旧版本。

**解决方案**：所有 `<script src>` 和 `<link href>` 加版本号 query：

```html
<script src="jquery-3.4.1.min.js?v=1786286012"></script>
<script src="c2runtime.js?v=1786286017"></script>
<script src="MobileControls.js?v=20260809"></script>
<script src="js/undertale_battle.js?v=120"></script>
```

版本号可以用当前 Unix 时间戳或手动更新。每次部署前记得改！

---

# 六、完整踩坑实录

## 坑 1：WebGL 上下文创建失败 → 画面空白

| 项 | 内容 |
|----|------|
| 现象 | `getContext('webgl')` / `getContext('webgl2')` 返回 null，rendering 代码崩溃或不渲染 |
| 根因 | 我们的浏览器环境 WebGL 支持有问题，或 C2 runtime 的 WebGL fallback 不完善 |
| 解决 | 强制 `_rt.enableWebGL = false`，让 C2 走 Canvas 2D 路径 |
| 位置 | `sans/index.html` 的 `cr_createRuntime` 后 |

## 坑 2：Keyboard 插件不存在 → 键盘输入无效

| 项 | 内容 |
|----|------|
| 现象 | 用户按 Z/X/C/方向键，游戏完全没反应 |
| 根因 | APK 里提取的 data.js 丢失了 Keyboard 插件的 Type 定义，C2 runtime 启动时无法实例化 |
| 解决 | 手动 `new cr.plugins_.Keyboard(R)` + `type.families = []` 必须显式赋值 |
| 位置 | `sans/index.html` 底部注入脚本 |
| 警告 | 忘了设 `type.families = []` 会报 `Cannot read properties of undefined (reading 'length')` |

## 坑 3：Debug 定时器轮询 → 无限血不生效

| 项 | 内容 |
|----|------|
| 现象 | `setInterval(80ms)` 恢复 HP，但用户挨打后还是进 GameOver |
| 根因 | C2 事件表在同一帧内完成"扣血→判死→切页面"，异步定时器插不进去 |
| 解决 | monkey-patch `setValue` + `Object.defineProperty` 双重拦截 |
| 提交 | `be3f082` |
| 教训 | **永远不要用轮询修同步问题** |

## 坑 4：gh-pages 和 main 不同步

| 项 | 内容 |
|----|------|
| 现象 | 本地测试没问题，线上 `emmioop.github.io` 还是旧版本 |
| 根因 | 只推了 main 没推 gh-pages，或者 gh-pages 有独立 commit |
| 解决 | `git checkout gh-pages && git reset --hard main && git push --force` |
| 教训 | **每次推送 main 后必须同步 gh-pages** |

## 坑 5：Service Worker 缓存旧文件

| 项 | 内容 |
|----|------|
| 现象 | 改了 index.html 加了新脚本，刷新还是加载旧的 |
| 根因 | SW 缓存了旧版本 HTML 和 JS |
| 解决 | 1) 更新 sw.js 里的 CACHE 版本号 2) 在 index.html 里禁用 SW 注册（当前方案） |
| 提交 | `459bf6d` |

## 坑 6：VitePress / CDN base href 冲突

| 项 | 内容 |
|----|------|
| 现象 | GitHub Pages 上资源 404 |
| 根因 | `<base href="/">` 或 CDN URL 不匹配 Pages 路径 |
| 解决 | 只用相对路径，禁用 base href |
| 教训 | **GitHub Pages 项目禁用任何 CDN base href 注入** |

## 坑 7：事件优先级搞反

| 项 | 内容 |
|----|------|
| 现象 | 剧情事件压过军事/经济决策事件 |
| 根因 | 早期 `story=2` 排在 `military=5` 前面 |
| 解决 | `military` 升为 2，`story` 降为 5 |
| 位置 | `js/game.js:923` |

## 坑 8：canvas.c2runtime 访问时机

| 项 | 内容 |
|----|------|
| 现象 | 在 `jQuery(document).ready` 里访问 `canvas.c2runtime` 报 undefined |
| 根因 | C2 runtime 是异步初始化的，`cr_createRuntime` 后才挂到 canvas 上 |
| 解决 | 用 `setInterval`/递归 `setTimeout` 轮询直到拿到 runtime |
| 模板 | `var R = canvas && canvas.c2runtime; if (!R) { setTimeout(fn, 200); return; }` |

---

# 七、第二 Boss 开发参考资料

## 7.1 为什么人家的模拟器加载那么快？

竞品 `caijiqaq.github.io/dusttale-debug/` vs 我们：

| 指标 | 我们 | 竞品 | 差距根因 |
|------|------|------|----------|
| 总大小 | **35 MB** | **1.0 MB** | 35× |
| PNG | 131 张 · 16 MB | 64 张 · 544 KB | 图片压缩到极致 |
| OGG | 77 个 · 14 MB | **0 个**（全 404） | 砍音频 |
| data.js | 814 KB | 244 KB | 事件表精简 3× |
| c2runtime | 1.1 MB | 743 KB | 同版本差不多 |

**核心优化手段（第二 Boss 可考虑）**：

| 手段 | 可省 | 备注 |
|------|------|------|
| data.js 里删除音频引用（让 loader 跳过） | -14 MB | C2 对 404 资源静默跳过，不阻塞游戏 |
| pngquant / tinypng 压缩 PNG | -10~12 MB | 像素图压缩效果好，画质影响小 |
| 砍无用事件组 | -500 KB | 小心别搞坏 game flow |
| 不做 Loader Layout | - | 省 layout 切换开销 |

## 7.2 Debug 模式架构可复用

第二 Boss 可以直接复用：

```
方案 A（C2 项目）：
  - 继续用 hook EventVariable.setValue + Object.defineProperty
  - 只要全局变量名对就行

方案 B（纯 JS/Canvas）：
  - 直接在扣血逻辑里 if (DEBUG.god) return
  - 参考 js/undertale_battle.js 的 hit 检测写法
```

## 7.3 C2 项目可快速获取

- Construct 2 免费版可导出（有广告 watermark，但可以裁掉）
- GitHub 上大量现成 C2 Undertale 同人：搜索 `construct 2 undertale boss`
- 下载后按本文档第 2 章的 hack 流程改：禁用 WebGL → 注入 Keyboard → hook Debug → Loader

## 7.4 开发 CheckList

每次搞新 Boss 时，按顺序做：

```
[ ] 下载 C2 导出项目
[ ] 检查 c2runtime.js 能否跑起来（禁用 WebGL 备选）
[ ] 注入 Keyboard 插件（type.families = [] 别忘）
[ ] 写 Debug hook（setValue + defineProperty）
[ ] 加 Loader 进度条 overlay
[ ] 禁用 / 保留 Service Worker 缓存
[ ] 加版本号 query 防缓存
[ ] 部署到 gh-pages（git reset --hard main + force push）
[ ] 线上测试（?debug=god 确认 hook 生效）
```

---

# 八、日志仓库结构说明

```
/workspace/logs-repo/
├── 技术实现完整手册.md   ← 你正在看的这个！总技术文档
├── 更新日志/CHANGELOG.md  ← 版本变更速查（Unreleased + 历史版本）
├── v0.4.2.md ~ v0.4.8.md  ← 各版本详细更新说明
├── DEV_LOG.md              ← 早期开发日志（已停止维护）
├── CHAT_LOG.md             ← 早期 AI 对话记录
├── 世界观_v2.1.md          ← TNO 改编世界观设定
├── 杉斯模拟器合集.zip       ← 竞品 APK 压缩包（留着做参考）
├── apk_extracted/          ← APK 解压内容
├── dusttale_extracted/     ← Dusttale 资源提取
└── 设计文档/               ← 其他设计稿
```

---

# 九、常用命令速查

## 本地启动 HTTP 服务器

```bash
# 启动主游戏 + Sans BOSS 战
python3 -m http.server 9327 --directory /workspace/tno-strategy-game

# 访问地址
# http://localhost:9327/index.html           ← 主游戏
# http://localhost:9327/sans/index.html       ← Sans BOSS 战
# http://localhost:9327/sans/index.html?debug=god  ← 调试模式
```

## 部署流程（一行版）

```bash
cd /workspace/tno-strategy-game && \
git add -A && git commit -m "update" && git push origin main && \
git checkout gh-pages && git reset --hard main && git push origin gh-pages --force && \
git checkout main
```

## Debug 验证 Console 命令

```js
// C2 版（Sans BOSS 战独立页）
var hp = document.getElementById('c2canvas').c2runtime.all_global_vars.find(v=>v.name==='HP');
hp.setValue(0); hp.data;   // 应返回 MaxHP
hp._godHooked;              // 应返回 true
window.__C2_DEBUG__;        // 查看当前 debug flag

// UndertaleBattle 版（TNO 内嵌战斗）
window.__BATTLE_DEBUG__;   // 查看 debug flag
```

## SW 缓存清除（用户端）

```js
// 打开 DevTools → Application → Service Workers → Unregister
// 或者 Console 执行：
navigator.serviceWorker.getRegistrations().then(rs => rs.forEach(r => r.unregister()));
// 然后清 site data，硬刷新
```

---

# 十、Git Commit 历史速览（关键提交）

| Commit | 说明 |
|--------|------|
| `be3f082` | **核心！** Debug 从定时器轮询升级为 hook setValue + defineProperty 双重拦截 |
| `b08115d` | 给 C2 runtime 注入 debug 模式脚本 |
| `810f973` | TNO UndertaleBattle 加 debug 模式（hit 检测里跳过 HP 扣减） |
| `9ae5e28` | 事件 tag 优先级修正（military 从 5→2，story 从 2→5） |
| `296d8c6` | 标题从"德衫"改成"dustsans" |
| `87e31a3` | 加 Loader 进度条 + 7 阶段告示 + 卡住检测 |
| `459bf6d` | 在 GitHub Pages 上禁用 SW 注册（防缓存冲突） |
| `b0ba093` | SW cache 版本升到 v3，清理旧缓存 |
| `e4a7970` | 加移动端虚拟控件 + 注入 Keyboard 插件 + 禁用 WebGL |
| `ef09a54` | 恢复页面 body 结构、X 按钮布局、英文对话、本地脚本引用 |
| `037ea2b` | 移除 CDN base href，纯本地引用修复 404 |
| `25df3a6` | MobileControls v2 原生重写（2074→429 行） |

---

# 十一、给下个账号的话

1. **先读这个文件**，比翻代码快 100 倍
2. **Sans BOSS 战的 C2 版是从别人仓库下载的**，我们没改 c2runtime.js 和 data.js 的核心逻辑（改了也没用，C2 runtime 闭包了）。所有功能都是通过 `index.html` 注入脚本实现的
3. **UndertaleBattle 版（js/undertale_battle.js）是我们自己写的**，纯 Canvas 2D，弹幕引擎抄了 c2-sans-fight 的 CSV 解析
4. **每次部署后务必同步 gh-pages**，否则线上永远是旧版
5. **Debug 模式的 hook 技巧是通用的**，以后搞 Construct 2 项目都能用
6. **积分不够时先存日志**，把踩过的坑记录下来，别让下个账号再踩一遍

---

*本文档最后更新：2026-08-09 · 对应主仓库 commit be3f082*
*下次更新：第二 Boss 开发时补充新的实现细节和踩坑记录*
