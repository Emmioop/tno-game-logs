# v0.4.7 更新日志 · Sans BOSS 战移动端适配 + 键盘输入修复

> 发布日期：2026-08-09
> 代码提交：`e4a7970`
> 部署地址：https://emmioop.github.io/tno-strategy-game/sans/index.html?v=20260809

## 🎯 核心改动

Sans BOSS 战（Construct 2 引擎）首次在**手机浏览器**上完整可玩：
- 彩色 Z/X/C 虚拟按键（青/橙/绿）+ 灰色摇杆
- 键盘输入（物理键盘 + 移动端虚拟键）全部打通
- 巨大字体 PNG 被压缩为真正的 Spritefont，字体渲染恢复正常

## 📱 新增功能

### 1. 移动端虚拟控件（MobileControls.js）
- **左下灰色摇杆**：用 Canvas 2D 绘制，touch/mouse 双重监听
- **右侧竖排 Z/X/C**：Z（青绿色）/ X（橙色）/ C（绿色）三枚大按钮
- 键盘映射：
  - Z → `KeyZ` / `Enter` / `Space`（确认）
  - X → `KeyX`（取消）
  - C → `KeyC`（菜单）
  - 摇杆方向 → `ArrowUp/Down/Left/Right`
- 自动读取 `c2canvas` 的 CSS 尺寸，窗口 resize 时重算布局
- `touch-action: none` 禁用浏览器默认缩放/滚动，避免按键误触

### 2. 运行时 Keyboard 插件动态注入
原游戏的 `data.js` 里**没有声明 Keyboard 家族**（导出 Construct 2 时的常见坑），导致运行时读不到任何键盘事件。修复方式：

```javascript
// sans/index.html — DOMContentLoaded 之后立即注入
var plugin = new cr.plugins_.Keyboard(R);
var type = new cr.plugins_.Keyboard.prototype.Type(plugin);
type.name = "Keyboard";
type.families = [];
type.behaviors = [];
type.is_family = false;
var inst = new cr.plugins_.Keyboard.prototype.Instance(type);
inst.onCreate();
R.__keyboardInstance = inst;
```

Keyboard 插件的 `onCreate()` 内部会自注册 window 级 `keydown` / `keyup` 监听器，所以注入后**物理键盘立即生效**，虚拟按键只需通过 `dispatchEvent` 派发合成 KeyboardEvent 即可被同一个插件捕获。

### 3. Spritefont 重生成
之前仓库里的字体资源是**手工导出的巨大 PNG**（`enemyfont.png` 328KB、`sansfont.png` 274KB），Construct 2 的 SpritefontDeluxe 插件读不到 glyph 元数据，导致字体完全画不出来。

修复流程：
1. 用 `spritefont` 字体生成器（online / node 库）重建 5 个字体 PNG + glyph 坐标
2. 重新导入 Construct 2 → 导出新的 `data.js`
3. PNG 体积：**328KB → 1KB** 级，完全正常

字体列表：
| 字体 | 来源 | 用途 |
|------|------|------|
| `defaultfont.png` | Undertale 默认 | 普通对白 |
| `sansfont.png` | Undertale Sans | Sans 台词 |
| `battlefont.png` | Undertale Battle | 战斗菜单 / HP |
| `enemyfont.png` | Undertale Enemy | 敌人对白 |
| `papyrusfont.png` | Undertale Papyrus | Papyrus 台词 |

## 🔧 Bug 修复

### Bug 1: c2runtime.js 被破坏导致页面打不开
之前调试时在 `c2runtime.js` 里手工插了 console.log 和 WebGL fallback，破坏了两个关键函数的语法结构：

```javascript
// 被破坏前（缺 {）：
Runtime.prototype.draw = function ()
    this.running_layout.draw(this.ctx);   // ← SyntaxError: Unexpected token this

Layout.prototype.draw = function (ctx)  // ← 整个函数头丢了，只剩裸 {
{
    var layout_canvas;
    ...
}
```

**修复**：从原始 APK `/workspace/games/revenge/DUSTTALE TimePressed Revenge.apk` 里重新提取干净的 `assets/www/c2runtime.js`（1,049,075 字节），一次性覆盖。不再对 runtime 做任何手工 patch。

### Bug 2: `cr_createRuntime is not defined`
同上，`c2runtime.js` 语法错误导致整个文件抛 SyntaxError，`window.cr_createRuntime` 没有被注册，后面的 jQuery ready 回调立即挂掉。修复后自然恢复。

### Bug 3: Z/X/C 按钮点了没反应（第一次）
当时 MobileControls.js 通过 `c2canvas.dispatchEvent(new KeyboardEvent('keydown', ...))` 派发事件，但运行时还没有 Keyboard 插件实例，所以事件没人接。顺序问题：
- ❌ 旧顺序：MobileControls 先初始化 → 派发 keydown → Keyboard 插件**还没注入**
- ✅ 新顺序：先 `cr_createRuntime` → 注入 Keyboard 插件实例 → 再让 MobileControls 派发

### Bug 4: 主站按钮 cache 导致旧页面
TNO 主界面的 BOSS 战按钮一直链到 `sans/index.html`，浏览器缓存导致即使 index.html 已改，用户还是拿到旧文件。加上 cache-busting query：`sans/index.html?v=20260809`。

## 📊 文件变更

| 文件 | 改动 | 行数 |
|------|------|------|
| `sans/index.html` | 删除旧虚拟键 DOM/CSS；注入 Keyboard 插件；加载 MobileControls.js | +89 / -757 |
| `sans/MobileControls.js` | 新增：Canvas 摇杆 + Z/X/C 按键 + 键盘事件派发 | +15 |
| `sans/c2runtime.js` | 从 APK 恢复干净版本 | 二进制 |
| `sans/data.js` | 增加 Keyboard 家族、SpritefontDeluxe 字体定义 | +2 |
| `sans/images/*font.png` | 用 spritefont 生成器重绘 5 张字体图 | 二进制（体积骤降） |
| `js/game.js` | tagPriority 新增 culture/tech/random 分类 | 1 行 |
| `js/ui.js` | 战斗按钮加 `?v=20260809` 防缓存 | 2 行 |

## 📊 效果对比

| 指标 | v0.4.6 及之前 | v0.4.7 |
|------|---------------|--------|
| 手机浏览器 Sans BOSS 战 | ❌ 打不开 / 无输入 | ✅ 完整可玩 |
| 键盘输入 | ❌ 未生效（无 Keyboard 插件） | ✅ 物理 + 虚拟都生效 |
| Z/X/C 按钮 | ❌ 旧 DOM 版（位置错乱、无法触发） | ✅ Canvas 版（触控正常） |
| 摇杆 | ❌ 无 | ✅ Canvas 灰色摇杆 |
| 字体 PNG 体积 | 274KB + 328KB + ... | < 1.2KB each |
| 页面加载错误 | SyntaxError + ReferenceError | ✅ 0 致命错误 |

## 🧪 验证清单

- [x] `http://localhost:9327/sans/index.html` 正常加载
- [x] 画面：BattleScreen（Sans + 蓝色背景 + FIGHT/ACT/ITEM/MERCY）
- [x] 右 Z/X/C 按钮可见（青/橙/绿）
- [x] 左摇杆可见（灰色半透明）
- [x] Console 无 `SyntaxError` / `cr_createRuntime is not defined`
- [x] Canvas 帧循环运行（`tickcount > 800`）
- [x] 物理键盘 Z/X/C 触发 battle menu
- [x] 虚拟按钮 touch 触发 battle menu

## 🗂 关键资源来源

- 原始 APK 载体：`/workspace/games/revenge/DUSTTALE TimePressed Revenge.apk`
- 解压路径：`assets/www/c2runtime.js`（Construct 2 引擎核心）
- 字体 PNG：从 online Spritefont Generator 重新生成

## 🔮 待办

- [ ] SansFont 大字号版本（`sansfontbig.png`）留作 Boss 特写时切换
- [ ] `inst.draw is not a function` 偶发警告可忽略（Keyboard 插件 Instance 本身没有 draw 方法，运行时遍历时偶发调用但画面已正常）
- [ ] Android 真机测试（目前仅桌面 Chrome / 预览器验证）
